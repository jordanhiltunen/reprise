# This config was automatically generated from your source code
# Stacks detected: deps:ruby:.,deps:rust:ext/coruscate,package_manager:gemspec:.
version: 2.1
orbs:
  ruby: circleci/ruby@2.0.1
jobs:
  test-ruby:
    # Install gems, run rake tests
    docker:
      - image: cimg/ruby:3.2-node
    steps:
      - checkout
      - ruby/install-deps
      - run:
          name: rake test
          command: bundle exec rake test
  test-rust:
    docker:
      - image: cimg/rust:1.70
    working_directory: ~/project/ext/coruscate
    steps:
      - checkout:
          path: ~/project
      - restore_cache:
          key: cargo-{{ checksum "Cargo.lock" }}
      - run:
          command: cargo test
      - save_cache:
          key: cargo-{{ checksum "Cargo.lock" }}
          paths:
            - ~/.cargo
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
workflows:
  build-and-test:
    jobs:
      - test-ruby
      - test-rust
    # - deploy:
    #     requires:
    #       - test-ruby
    #       - test-rust
